services:
  sonarr:
    image: lscr.io/linuxserver/sonarr:latest  
    container_name: sonarr
    security_opt:
      - no-new-privileges:true
    restart: "unless-stopped"
    profiles: ["media", "sonarr", "all"]
    networks:
      - t3_proxy
    ports:
      - "8989:8989"
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${DOCKERDIR}/appdata/sonarr:/config
      - ${DOCKERDIR}/appdata/nzbget/data/completed:/data/completed # for completed downloads
      - media_tvshows_rw:/tv
    environment:
      TZ: ${TZ}
      PUID: ${PUID}
      PGID: ${PGID}
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.sonarr.rule=Host(`sonarr.${DOMAINNAME_1}`)"
      - "traefik.http.routers.sonarr.entrypoints=websecure-internal"
      - "traefik.http.routers.sonarr.tls=true"
      - "traefik.http.services.sonarr.loadbalancer.server.port=8989"
      - "traefik.docker.network=t3_proxy"

volumes:
  media_tvshows_rw:
    name: media_tvshows_rw
    driver: local
    driver_opts:
      type: "nfs"
      o: "nolock,soft,rw"
      device: "192.168.1.112:/mnt/datastore/media/TV Shows"